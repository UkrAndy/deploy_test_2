<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/products/list.css" />
    <script src="/javascripts/RequestManager.js"></script>
    <script>
      function onFileInput(event) {
        RequestManager.handleFileSelect(event, '#imgPreview')
      }
    </script>
  </head>
  <body>
    <form
      action="/products<%= product ? '/' + product.id : '' %>"
      enctype="multipart/form-data"
      method="post"
    >
      <label>
        Назва товару
        <input
          type="text"
          name="title"
          value="<%= product ? product.title : '' %>"
          required
        />
      </label>
      <hr />
      <label>
        Кількість товару
        <input
          type="number"
          name="amount"
          value="<%= product ? product.amount : '' %>"
          required
        />
      </label>
      <hr />
      <label>
        Ціна одиниці товару
        <input
          type="number"
          name="price"
          value="<%= product ? product.price : '' %>"
          required
        />
      </label>
      <hr />
      <div class="prod-container">
        <label>
          Зображення товару <input type="file" name="prodImg" accept="image/*"
          onchange="onFileInput(event)" <%= product ? '' : 'required' %> />
        </label>
        <img
          id="imgPreview"
          src="<%= product && product.imgSrc ? product.imgSrc : '/photo-not-available.jpg' %>"
          alt="Product Image Preview"
          class="prod-image"
        />
      </div>
      <hr />
      <button type="submit"><%= product ? 'Оновити' : 'Створити' %></button>
    </form>
  </body>
</html>
